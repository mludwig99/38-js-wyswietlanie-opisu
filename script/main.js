let paragraf = document.getElementById('opis');
let button = document.querySelector('button');
button.onclick = function() {
    paragraf.textContent = 'Opis produktu (ang. product description) to tekst znajdujący się w karcie produktu, który wyjaśnia użytkownikom...'
}